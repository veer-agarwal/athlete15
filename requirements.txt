# Phase 1
requests==2.32.3
python-dotenv==1.0.1
feedparser==6.0.11

# Phase 2
python-telegram-bot==21.6
APScheduler==3.10.4

# Phase 4 (WHOOP OAuth)
whoop==1.0.1

# Phase 7 (athletics .ics)
# Bumped from 6.1.0: caldav's Calendar.search()/date_search() go through
# icalendar_searcher, which imports `icalendar.error`, a module only added in
# icalendar 6.2.0. On 6.1.0 every caldav search() call raised ImportError.
# See src/sources/calendar.py's module docstring.
icalendar==6.3.2

# iCloud calendar (CalDAV)
caldav==3.2.1
vobject==0.9.9

# Dev
pytest==8.3.3
